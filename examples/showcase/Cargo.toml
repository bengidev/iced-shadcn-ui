[package]
name = "showcase"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
iced = { version = "0.14", features = ["advanced", "tokio", "wgpu"] }
lucide-icons = { version = "1", features = ["iced"] }
